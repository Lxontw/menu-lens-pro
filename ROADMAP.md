# 🚀 MenuLens Pro 進化路線圖 (Roadmap)

本文件記錄 MenuLens Pro 的未來開發方向與功能構想，旨在將其從單純的翻譯工具演進為全方位的 AI 視覺飲食助手。

## 📍 當前版本：v0.3.0 (成品 POC)
- [x] Gemini 多模態 AI 翻譯
- [x] 基礎食物圖片匹配 (LoremFlickr)
- [x] 過敏原與飲食標籤辨識
- [x] UIBridge 邏輯解耦架構
- [x] 本地收藏夾系統

---

## 🛠️ 待開發進階功能

### 1. 真正的 AR 疊加 (Canvas Overlay)
**目標**：將翻譯結果直接呈現在原圖的對應位置，而非獨立的卡片。
- **技術路徑**：
    - 優化 Gemini Prompt，要求回傳每個項目的 `bounding_box` (座標資訊)。
    - 在前端使用 HTML5 Canvas 建立透明層，根據座標繪製半透明遮罩與翻譯文字。
- **價值**：極大化視覺直觀感，達成類似 Kuli Kuli 的 AR 翻譯體驗。

### 2. 提升圖片匹配的精準度
**目標**：將「示意圖」提升為「真實參考圖」。
- **技術路徑**：
    - 評估並整合 `Google Custom Search API` 或其他專業美食 API。
    - 實作更精準的搜尋詞提取邏輯（例如：將「鯖魚」精確化為「grilled mackerel japanese style」）。
- **價值**：讓使用者能更準確地確認點餐內容，減少誤點風險。

### 3. 美食地圖與旅程紀錄 (Dining Journey)
**目標**：將「收藏夾」轉化為「美食回憶錄」。
- **技術路徑**：
    - 整合 `Geolocation API`，在儲存翻譯結果時自動記錄經緯度與時間戳。
    - 實作簡單的地圖視圖（如 Leaflet 或 Google Maps），將收藏的美食標記在圖上。
- **價值**：賦予工具「紀錄」屬性，讓使用者能回顧旅行中的飲食足跡。

### 4. 遷移至 Stitch UI 框架
**目標**：從 Vanilla JS 遷移至更現代的開發框架，提升維護性與效能。
- **技術路徑**：
    - 利用現有的 `UIBridge` 接口，將 `CoreLogic` 直接移植。
    - 使用 Stitch 重新構建 UI 組件，實現更複雜的狀態管理與動畫效果。
- **價值**：將 POC 轉化為可生產等級的 App 結構，提升整體 UX 流暢度。
