// src/ui/components.js

/**
 * This file will contain functions that return HTML strings for small, reusable UI elements.
 * This will help in making the code more modular and reusable.
 * For now, it's a placeholder.
 */
export const components = {
    // Example:
    // card(title, content) {
    //     return `<div class="card"><h2>${title}</h2><p>${content}</p></div>`;
    // }
};
