# MenuLens Pro 功能規格書 v2.1

## 目標
將 MenuLens Pro 從「菜單翻譯工具」擴充成「可直接帶去店內使用的點餐、分帳、歷史紀錄工具」，並逐步延伸成「掃描收據/單據 → 自動整理帳本 → 統計分析」的個人消費管理工具。

本專案的核心目的分成三層：
1. 拍菜單、翻譯、點單，產生可給服務人員確認的訂單，並同步保留個人紀錄與分帳資訊。
2. 擴充到可拍攝購物單據/收據，辨識購買方式、內容與金額，整理進可自訂分類的帳本。
3. 帳本資料可做圓餅圖、分類分析與花費統計，幫助使用者回顧消費結構；其餘帳本功能則依一般記帳 App 的常見需求補齊，例如收支新增/編輯/刪除、分類管理、月份檢視、搜尋與篩選、總覽與明細查看等。

本次重點仍先聚焦在店內點餐流程：
1. 掃描後先產出可檢查的菜單清單，使用者確認後再加入購物車。
2. 由購物車生成給服務人員看的訂單預覽，並可轉成分帳紀錄。
3. 訂單與分帳結果都要能保存成歷史記憶，之後可重新查看與延伸。

## UI 參考與產品差異化

### 參考 App 有、我們目前還沒有的
- 更完整的帳本首頁摘要：一進去就看到總支出、本月支出、分類排行與最近紀錄。
- 更成熟的多帳本列表：可直接看到多個帳本名稱、用途與快速進入/編輯/歸檔入口。
- 更完整的分析頁：圓餅圖、趨勢圖、分類明細、預算進度等分層清楚。
- 更完整的手動記帳表單：可直接新增、編輯、刪除一筆紀錄。
- 更成熟的日期/月份視圖：可從月曆快速切換與回看當月支出。

### 我們有、參考 App 沒有的
- 菜單拍照翻譯與點單流程：可從菜單掃描直接變成訂單。
- 服務人員訂單預覽：可在手機上直接展示給店員看。
- 分帳紀錄與人員分攤：一張訂單可直接轉成分帳。
- 掃描後自動進購物車 / 訂單同步：把辨識結果直接串成可操作流程。
- 菜單、收據、帳本三合一：同一套掃描能力可延伸到店內點餐與日常記帳。

## 功能分區圖 / 產品藍圖

### A. 掃描中心
- 菜單掃描：拍照辨識、翻譯、選取品項、加入購物車。
- 收據掃描：辨識商店、日期、金額、品項、幣別與原始辨識文字，並準備匯入帳本。
- 收據掃描不預設要求付款方式欄位；若 OCR 沒辨識到付款方式，就不要硬塞或顯示假資料。
- 收據與菜單辨識都要保留原文與翻譯並存的資料結構，避免只剩單一語言。
- 掃描 UX：相機失敗要有備援，上傳照片要明確可見，不能讓流程卡死；拍照辨識失敗時要能回到重新拍攝或上傳。

### B. 點單與訂單中心
- 結果清單：讓使用者先檢查辨識內容，再決定是否加入購物車。
- 購物車：同步數量、刪除、收藏、備註與總額。
- 訂單預覽：把購物車轉成服務人員可直接看的訂單格式。
- 歷史訂單：可回看過往點單內容與訂單紀錄。

### C. 分帳中心
- 從訂單直接建立分帳。
- 可新增人員、刪除人員、平均分帳或自訂分配。
- 分帳結果要可保存，可重新開啟。
- 分帳可與特定帳本或特定情境綁定。

### D. 帳本中心
- 多帳本管理：日常生活、旅行、家庭開銷、工作、娛樂等。
- 單一帳本內有自己的分類、標籤、紀錄與摘要。
- 帳本可建立、切換、歸檔、重命名、調整封面與用途。
- 收據/單據可直接寫入指定帳本。

### E. 分析中心
- 圓餅圖、趨勢圖、分類排行、預算進度。
- 月份視圖、週期視圖、跨帳本總覽。
- 搜尋與篩選：依日期、分類、金額、店家、標籤等查看。
- 首頁摘要：顯示本月支出、最大分類、最近紀錄、待修正項目。

### F. 維護與資料中心
- 匯出 / 匯入：支援單一帳本或全資料備份。
- 重複資料偵測：降低重複掃描造成的髒資料。
- OCR 後人工修正：確保辨識資料可以被人修正後再入帳。
- localStorage 相容性：所有新功能都要兼容舊資料。

### G. 模組化架構原則
- 產品要拆成清楚的功能模組，不把掃描、訂單、分帳、帳本、分析、設定混在同一層。
- 每個模組應該有自己的資料責任、UI 責任與操作入口，避免互相污染。
- 模組之間只透過明確的狀態與事件溝通，避免直接依賴彼此的 DOM 細節。
- 新功能優先新增在對應模組，不要把所有邏輯塞回單一大檔案。
- 舊資料與舊功能要可向後相容，模組拆分不能破壞現有 localStorage 資料。

### 模組拆分建議
- 掃描模組：負責相機、上傳、OCR、結果解析、失敗備援。
- 訂單模組：負責結果清單、購物車、訂單預覽、歷史訂單。
- 分帳模組：負責參與者、分攤規則、分帳結果保存。
- 帳本模組：負責多帳本建立、切換、歸檔、收支紀錄、分類、標籤。
- 分析模組：負責圖表、統計、趨勢、搜尋與篩選。
- 資料模組：負責 localStorage、匯出匯入、版本相容、重複資料偵測。
- 設定模組：負責 API Key、模型、幣別、偏好設定、預設帳本、預設掃描模式、提醒與介面偏好。

### 模組化驗收標準
### 檢測與檢驗機制
- 每個模組在進入下一階段前，都要有對應的檢測與驗證項目。
- 需要檢測的面向至少包含：流程可達性、資料同步、UI 狀態、localStorage 持久化、錯誤回退。
- 檢測不只看功能有沒有出現，還要確認使用者能不能順利完成下一步，不能卡在中途。
- 掃描、訂單、分帳、帳本、分析各自都應有獨立驗證清單。
- 新功能上線前，必須確認不破壞既有資料與既有流程。

### 檢驗標準
- 每個主要流程都要有明確的驗收條件，而不是只看畫面有沒有出來。
- 驗收至少包含：
  - 是否能正常完成操作
  - 是否有 fallback / 返回路徑
  - 是否與資料狀態同步
  - 是否能持久化保存
  - 是否能重新開啟並讀回
- 若涉及掃描與辨識，還要檢驗錯誤情境：相機失敗、OCR 失敗、格式解析失敗、重複資料。
- 若涉及帳本與統計，還要檢驗圖表數值、分類計算與總額一致性。

### 子代理協作原則
- 當一個需求橫跨多個模組時，可交給子代理分工處理，例如：一個負責掃描流程、一個負責帳本分析、一個負責驗證與整理規格。
- 子代理的任務要切得明確，只負責單一子題，不要讓它同時改太多方向。
- 子代理產出的內容要回到主規格統整，避免各自為政。
- 子代理適合做：資料整理、UI 參考歸納、流程拆解、檢測清單、風險盤點。
- 主代理負責最後整合、排序優先順序與確認是否符合產品方向。

### 子代理驗收標準
- 子代理輸出必須可回收成 spec 或執行清單。
- 子代理不得改變產品方向，只能補充、拆解、驗證與提出風險。
- 主代理在採納前要能快速判斷其結論是否與主 spec 一致。

- 進階分類與標籤：除了固定分類，支援自訂標籤，例如聚餐、出差、日常、健康、外送。
- 月份 / 週期視圖：提供本月、上月、每週、年度等檢視，讓統計不只停留在單次記錄。
- 預算與提醒：可設定每月分類預算，接近上限時提醒。
- 匯出 / 匯入：支援 JSON 或 CSV 匯出與匯入，降低 localStorage 風險，也方便備份與換機。
- 重複資料偵測：掃描收據或菜單時，對相同日期、相同總額、相似店名的紀錄提供重複提示。
- 記錄詳情頁：每筆紀錄可點進去查看完整內容，包括原始掃描資料、修正後資料、分類、標籤、備註與建立時間。
- 固定支出 / 週期性支出：支援房租、訂閱、保費等重複性支出。
- 多幣別支援：考量日圓與其他幣別場景。
- 帳本不是單一混合總帳，而是支援多帳本 / 多情境分類管理，例如日常生活、旅行、家庭開銷、工作、娛樂等。使用者可以自行建立帳本名稱與用途，例如「2026-06 日本旅行」，專門記錄該次旅行的開銷。
- 每個帳本可獨立保存收支紀錄、分類、標籤、統計與圖表，彼此之間互不混淆，但也可在總覽中合併檢視。
- 圖表擴充：除了圓餅圖，未來可加入長條圖、趨勢圖、分類排行。
- 離線可用：維持純前端架構時，盡量讓核心功能可離線使用。
- 首頁總覽：顯示本月總支出、最大分類、最近一筆、待修正項目等摘要資訊。

## 設計原則
- 全程維持純前端單頁應用，不新增後端。
- 所有關鍵資料仍以 localStorage 持久化。
- 手機優先，操作不能依賴桌面式 prompt / confirm。
- 掃描流程必須有明確主次動作、明確返回路徑、明確失敗備案。
- UI 上不要讓使用者猜：每一步都要知道現在在做什麼、下一步是什麼。
- Spec 採「主規格 + 本次修正」模式：主規格維持穩定，本次只新增或修正差異，避免每次重讀整份文件造成浪費。
- 每次修正都要明確標註：修改範圍、介面變動、資料結構變動、驗收標準。
- UI 必須可拆成獨立呈現層與資料/狀態層，為未來與 Stitch 對接預留介面邊界與資料契約。

### G. UI 分層與 Stitch 對接原則
- UI 必須視為可替換的呈現層，不可把商業邏輯、資料同步、流程控制全混在同一段 DOM 繫結裡。
- 每個主要頁面都應先有一層穩定的 view model / view props 介面，再由渲染函式輸出畫面。
- 事件處理與畫面渲染要分開，避免未來 Stitch 接手時必須先拆掉大量耦合邏輯。
- UI 互動要透過明確的 action / handler 契約傳遞，而不是依賴散落的全域變數。
- 同一個頁面若有多個狀態（loading、empty、error、ready），應統一由同一個呈現層根據狀態渲染，不要在多個地方重複拼接。
- 若未來需要 Stitch 對接，應能只替換 view layer 而不破壞 core services、storage、state machine。
- 任何新增 UI 元件都要能對應到明確的資料來源、狀態變化與返回路徑。
- 共享元件（如卡片、按鈕列、空狀態、狀態標籤）應集中管理，避免每頁各自實作造成樣式與行為分裂。
- 掃描會話（ScanSession）：一次拍照或上傳所形成的辨識流程，保存來源、模式、時間、結果與錯誤資訊。
- 菜單項目（MenuItem）：菜單辨識後的單一品項，保存原文、翻譯、價格、描述、標籤、過敏提醒；原文與翻譯必須可同時存在，不可互相覆蓋。
- 收據項目（ReceiptItem）：收據辨識後的單一項目，保存原始文字、翻譯文字、價格、數量、分類、幣別與是否已轉換。
- 收據辨識結果（ReceiptResult）：保存店名、日期、總額、原始幣別、顯示幣別、換算金額、原始辨識文字與支付資訊（若有；缺省時不得硬補）。
- 購物車項目（CartItem）：使用者確認後要進入購物車的項目，保存數量、選取狀態與調整結果。
- 訂單（Order）：由購物車整理出的正式單據，保存店名、時間、總額、明細、備註與狀態。
- 分帳紀錄（SplitBill）：由訂單轉換而來，保存參與者、分配方式、每人應付、調整紀錄。
- 帳本（LedgerBook）：一個獨立情境帳本，如日常生活、旅行、家庭開銷等。
- 收支紀錄（Transaction）：帳本內最基本的紀錄單位，保存日期、金額、分類、標籤、來源、備註、原始幣別、換算來源與原始掃描結果。
- 分類（Category）與標籤（Tag）：用於統計、搜尋、分析與分類管理。
- 分析快照（AnalysisSnapshot）：用於快速呈現圖表或首頁摘要的彙總資料，可由交易推導。

### I. 流程狀態機
- 掃描流程：待機 → 啟動相機 / 上傳 → 辨識中 → 結果檢查 → 加入購物車 → 訂單預覽。
- 訂單流程：訂單預覽 → 分帳建立 → 分帳調整 → 儲存歷史。
- 帳本流程：帳本選擇 → 新增/編輯交易 → 分類/標籤 → 統計分析 → 匯出/匯入。
- 每個流程都必須有「返回前一步」與「取消/離開」機制，避免形成死路。
- 每個流程的 UI 狀態需明確對應資料狀態，不可出現畫面顯示與資料不一致。

### J. 版本與資料遷移策略
- 所有 localStorage 資料結構變更都要可向後相容，舊資料至少要能被讀取或安全轉換。
- 新增欄位時以「可選欄位」優先，避免一次改壞舊資料。
- 若未來需要大幅調整資料結構，需先定義版本號與遷移規則，再動資料層。
- 任何遷移失敗都要回退到可讀狀態，不可讓使用者資料消失。
- 重要資料（訂單、分帳、帳本）應考慮提供重建能力：可從原始掃描結果或現有交易重建摘要。

### K. 產品驗證與發布關卡
- 每次大改版都要經過：規格確認 → 模組拆分確認 → 流程驗證 → 資料相容驗證 → UI 體驗驗證。
- 驗證不只看畫面，還要確認：
  - 是否能從入口走到結尾
  - 是否有空狀態與錯誤回退
  - 是否有資料持久化
  - 是否有跨模組同步
  - 是否沒有明顯半成品入口暴露在主路徑
- 若某功能尚未完整，必須明示為「開發中」或先從主路徑移除，不能默默留在主畫面造成混淆。
- 大改版發布前，應先用關鍵情境走查：菜單掃描、收據掃描、帳本新增、分帳儲存、歷史回看、圖表切換。

### L. 主目標與長期路線
- 第一層：菜單點餐與分帳，讓使用者在店內就能完成掃描 → 點單 → 分帳 → 保存。
- 第二層：收據/單據掃描，讓日常花費可快速進入帳本。
- 第三層：帳本分析，讓使用者可以做分類、圖表、趨勢與預算管理。
- 最終目標：形成一個從「拍一下」到「看懂花費」的完整閉環，而不是只是一個單點掃描工具。


### M. 主導航與未完成功能處理
- 主導航只保留已完成、可形成完整閉環的核心功能：掃描/記錄、帳本、生活工具（已完成項目）、設定等。
- 未完成或半成品功能不得與主流程混排在同一層級的主要入口，避免使用者誤以為已完成。
- 若功能暫時保留，必須明確標記為「開發中」或移入次級區域，不可只露出按鈕卻沒有完整流程。
- 例如 unit、reminder 這類未完成工具，要麼補完整，要麼在主 UI 上降級處理，避免破壞產品可信度。
- 任何主導航項目都應對應一個清楚可達的結果頁或詳情頁，不可點進去只剩提示文字。

### N. 命名與內容表述規範
- 使用者看見的名稱要直白，不用內部術語堆砌。
- 帳本名稱、訂單名稱、分帳名稱都應支援使用者自訂，並以情境為主。
- 舉例："2026-06 日本旅行"、"家用超市"、"與朋友聚餐" 這種名稱要優先支持。
- 統計名稱與分類名稱應使用使用者易懂的語言，必要時再附英文或備註作為輔助。
- UI 文案要短、清楚、可行動，不能只寫抽象提示。

### O. 資料成長與效能原則
- localStorage 是目前的唯一持久化層，因此資料結構要考慮成長與壓縮。
- 統計與圖表應優先由現有交易資料即時計算，必要時才存快照，避免重複儲存太多冗餘資料。
- 列表、圖表、歷史與搜尋需能在資料量增加時保持可用，不可讓 UI 因單一帳本太大而明顯卡頓。
- 匯出/匯入與歷史資料設計都要預留未來外部備份或遷移的可能性。

### P. 設定頁面 / 設定中心
- 設定頁面必須是一個獨立、清楚、可回頭的模組，不只是零散選項集合。
- 設定頁面要採手機優先單欄式版面，底部固定主要操作列，避免使用者滑到底才找得到儲存。
- 設定頁面要分層：全域設定、帳本設定、掃描設定、分析設定、資料管理。
- 設定首頁需提供快速入口卡片，讓使用者一眼找到「API / 模型」、「帳本」、「掃描」、「資料」這四大高頻區塊。
- 全域設定至少包含：API Key、Gemini 模型、語言 / 幣別、主題、通知偏好、預設首頁。
- 帳本設定至少包含：預設帳本、帳本模板、分類預設、預算提醒、歸檔 / 顯示設定。
- 掃描設定至少包含：預設掃描模式（菜單 / 收據）、相機行為、OCR 後自動進結果頁或直接進預覽的偏好。
- 分析設定至少包含：圖表預設、時間範圍預設、是否顯示分類排行 / 趨勢圖 / 預算進度。
- 資料管理至少包含：備份、匯出、匯入、清除快取、版本相容檢查、重複資料處理。
- 每個設定區塊都要有簡短說明，明確告訴使用者「這個設定會影響什麼」。
- 設定變更若可立即套用就立即套用；若需要重載或重新進入流程，必須明確提示，不可默默生效。
- 所有設定都需有儲存狀態提示，至少包含：未變更、已變更未儲存、已儲存成功、儲存失敗。
- 設定頁面要支援復原：若改錯或不確定，可回復預設或回到上一組設定。
- 設定頁面應提供「回到上次儲存」與「回復預設」兩種恢復方式，避免只剩單向覆蓋。
- 設定頁面應避免只提供技術名詞，要以使用者能理解的語言呈現，例如「預設進入日本旅行帳本」而不是單純「defaultBookId」。
- 設定項目不能太碎，應把高頻使用項目放前面，低頻管理項目放後面。
- 若某設定只影響單一帳本或單一流程，優先放在該模組內的設定，而不是全部塞到全域設定頁。
- 設定頁的清單與開關要遵守一致性：同一類操作用相同元件，不同風險等級的項目要有明顯視覺區別。
- 設定頁要有明確返回路徑：從設定頁可回到原本流程，也可回到首頁，不可讓使用者迷路。
- 設定頁也要有檢測：至少測試儲存、讀取、還原、跨頁生效、localStorage 相容。
- 設定頁的驗收標準：使用者在手機上 3 次內可找到預設帳本與 Gemini 模型設定，且可確認儲存是否成功。

#### P1. 設定首頁結構
- 設定首頁先顯示「目前狀態卡」：目前帳本、目前掃描模式、目前模型、目前幣別，讓使用者知道現在環境是什麼。
- 設定首頁下方放四個主要入口卡：一般設定、掃描設定、帳本設定、資料管理。
- 入口卡要有一句短說明，例如「調整 API / 模型與顯示語言」、「設定預設帳本與分類」。
- 設定首頁底部固定兩個動作：返回首頁、儲存變更；若有未儲存狀態，儲存鍵需明顯高亮。

#### P2. 一般設定頁
- 一般設定頁包含：API Key、Gemini 模型、語言、幣別、主題、通知、預設首頁。
- API Key 與 Gemini 模型放在最上方，因為這是掃描功能能否工作的核心。
- Gemini 模型需顯示目前使用的模型名稱與一行說明，例如「影響掃描準確率與回應速度」。
- 語言 / 幣別區塊要支援快速切換，並即時反映在價格與分析標示。
- 主題設定只提供少數清楚選項，不要讓使用者在太多色彩主題中迷路。

#### P3. 掃描設定頁
- 掃描設定頁包含：預設掃描模式、相機啟動方式、掃描後自動流程、失敗備案。
- 預設掃描模式至少兩種：菜單翻譯、收據整理。
- 相機啟動方式要能選擇「進頁即開相機」或「先確認再開」。
- 掃描後自動流程需可選：直接進結果頁、先進預覽頁、失敗時改上傳圖片。
- 掃描設定頁必須顯示失敗備案說明，例如「若相機無法使用，可改用上傳圖片」。

#### P4. 帳本設定頁
- 帳本設定頁包含：帳本清單、預設帳本、帳本模板、分類預設、預算提醒、歸檔設定。
- 帳本清單以卡片列出，每個帳本顯示名稱、用途、最近更新時間。
- 預設帳本可直接選擇，並明確標示「下次預設進入的帳本」。
- 帳本模板用來快速建立新帳本，例如旅行、日常、家庭、工作。
- 分類預設需可設定常用分類順序，方便記帳或分析時快速選取。

#### P5. 資料管理頁
- 資料管理頁包含：備份、匯出、匯入、清除快取、版本檢查、重複資料處理。
- 匯出與備份要分清楚用途：匯出是給使用者帶走，備份是恢復資料用。
- 匯入流程要先顯示將覆蓋或合併哪些資料，不可直接執行。
- 版本檢查要先告訴使用者舊資料是否可讀，若不可讀要提供明確修復路徑。
- 清除快取不可誤傷正式資料，按鈕文字與風險提示要明顯。

#### P6. 設定頁的共通元件
- 所有頁面共用同一套返回、儲存、取消、重設元件，避免每頁按鈕語意不同。
- 高風險操作要二次確認，但不能用原生 confirm()。
- 重要設定項可用切換開關、下拉選單、單選卡片三種基本元件為主，避免過度複雜。
- 設定頁所有更動都要進入「未儲存」狀態，直到成功寫入 localStorage。

#### Q. 主導航與頁面地圖
- 主導航要清楚對應產品的四個核心任務：掃描、點單、分帳、歷史；設定另作獨立入口。
- 首頁不是功能垃圾桶，首頁只放高頻入口與目前狀態，不把所有功能一次塞滿。
- 首頁高頻入口至少包含：開始掃描、開啟最近一次訂單、查看歷史帳本、進入設定。
- 底部導航若採用分頁式架構，分頁名稱要短且穩定，例如「掃描」「訂單」「分帳」「歷史」「設定」。
- 每個分頁都要有自己的明確首頁，不要點進去只有一個空白容器。
- 掃描流程的返回路徑：結果頁 → 掃描頁 → 首頁，使用者可在任一層返回上一步，不會被困住。
- 訂單流程的返回路徑：訂單預覽 → 購物車/結果頁 → 首頁，且送出前可修改。
- 分帳流程的返回路徑：分帳頁 → 訂單預覽 → 訂單頁 → 首頁，避免分帳後無法回頭。
- 歷史流程的返回路徑：歷史列表 → 歷史詳情 → 對應訂單 / 分帳 → 首頁。
- 設定頁的返回路徑：設定頁 → 首頁或原本入口頁，不得只靠瀏覽器返回。
- 主導航中任何不可用的功能都不能留在第一層做同等級入口；若尚未完成，必須降級至次級區塊或標示預覽狀態。

#### R. 空狀態與失敗狀態文案
- 空狀態不是單純顯示「沒有資料」，要告訴使用者下一步可以做什麼。
- 空狀態至少要分三種：沒有掃描結果、沒有訂單、沒有歷史紀錄。
- 沒有掃描結果時應引導使用者去拍照或上傳圖片，而不是只顯示空白圖示。
- 沒有訂單時應提示可以先從掃描建立菜單，或從歷史複製上一筆訂單。
- 沒有歷史紀錄時應提示「完成一次訂單後，這裡會自動保存」。
- 相機權限失敗時要顯示可執行備案，例如「改用上傳圖片」與「重新授權相機」。
- API Key 不存在或 Gemini 失敗時，文案要講清楚是設定問題還是服務問題，不可只顯示「發生錯誤」。
- 儲存失敗時要保留使用者剛剛的修改內容，不能因失敗而清空表單。
- 失敗文案要短、明確、可行動，格式上優先採「發生了什麼 → 可以怎麼做」。
- 若功能暫時不可用，要明確標示原因與替代路徑，不可讓入口看起來像壞掉。

#### S. 匯出 / 分享 / 備份功能
- 匯出、分享、備份要拆成三種不同語意，不可混在同一個按鈕上讓使用者猜。
- 匯出是把資料整理成可下載檔案，目的是移轉、保存或後續編輯。
- 分享是把目前畫面或結果快速傳給別人，目的是即時溝通，不一定等同完整資料匯出。
- 備份是保存可還原的完整資料快照，目的是日後復原，不是日常分享用途。
- 匯出功能至少支援：單筆訂單、單一帳本、全部資料三種範圍。
- 匯出格式至少要有一個人類可讀格式與一個可還原格式，避免只有單一用途。
- 分享功能要支援把訂單預覽、分帳結果、掃描結果快速產出分享內容，內容要適合手機通訊軟體轉傳。
- 分享內容需可選擇是否包含價格、備註、分帳人名、總額，避免資訊外洩。
- 備份功能要明確顯示備份內容範圍、最後備份時間、是否可還原、是否與目前版本相容。
- 匯出與備份流程都要先給使用者預覽，讓使用者知道檔案裡會有什麼。
- 匯出成功後要提供明確下一步，例如下載、分享、複製連結或保存到檔案。
- 備份成功後要顯示可驗證的結果，例如備份時間與資料筆數。
- 還原前必須顯示風險提示，並說明會覆蓋哪些資料或採合併方式。
- 若備份檔與目前版本不相容，要先阻止直接還原，並提供版本檢查與修復說明。
- 匯出 / 分享 / 備份的按鈕文字要清楚區分，例如「匯出訂單」、「分享給朋友」、「建立備份」。
- 這三種功能都要保留完成後回到原頁面的路徑，不能讓使用者執行完就失去上下文。

#### T. 訂單詳情頁
- 訂單詳情頁要把「看懂內容」放在第一位，不只顯示總額，還要顯示品項、數量、單價、小計與備註。
- 訂單詳情頁應有明確狀態：草稿、已確認、已送出、已轉分帳、已封存。
- 草稿狀態可自由修改品項與數量，並可返回結果頁或購物車頁。
- 已確認狀態仍可建立分帳，但修改動作要先提醒會影響分帳結果。
- 已送出狀態要保留送出時間與送出內容，方便日後回查。
- 訂單詳情頁主按鈕要清楚區分：返回修改、確認送出、進入分帳、分享、匯出。
- 若訂單內容太長，品項清單要可折疊，避免總額被埋沒。
- 若有多個人共點，訂單詳情頁要支援標示誰點了什麼，方便後續分帳。
- 訂單詳情頁要有快速返回路徑，至少可回到掃描結果、購物車與首頁。

#### U. 分帳頁的人員管理
- 分帳頁要先顯示總金額與目前參與人數，讓使用者先理解分帳規模。
- 人員管理要支援新增、刪除、重新命名、排序。
- 預設分帳模式至少包含：平均分帳、按品項分配、手動調整。
- 每個人都要能看到自己目前分到哪些品項與金額，避免只看到最後總數。
- 分帳頁要支援常用人員快速加入，例如最近一起用餐的人可一鍵加入。
- 分帳結果一旦改動，總額、每人應付與差額要即時重算。
- 分帳頁需保留「回到訂單」與「保存分帳結果」兩個核心動作，不可只剩單一送出按鈕。
- 若使用者中途離開，未完成的分帳草稿應保存，重新打開可以續做。
- 分帳頁要有清楚空狀態：尚未加入任何人時，直接引導新增第一位成員。

#### V. 歷史紀錄的複製再用
- 歷史紀錄不是只有查詢用途，也要能重複利用。
- 每筆歷史紀錄應支援「再次使用這張訂單」的操作，將舊訂單複製成新草稿。
- 複製時可選擇保留品項內容、保留分帳人員、保留備註，並允許使用者重新編輯。
- 歷史詳情頁應顯示：日期、帳本、店家、品項、總額、分帳結果、備註、來源。
- 歷史頁要能快速切回對應的訂單或分帳頁，而不是只有靜態檢視。
- 歷史列表至少支援依日期、帳本、店家或金額篩選。
- 常用歷史紀錄應可收藏或標記為常用模板，方便重複聚餐或固定支出情境。
- 從歷史複製出來的新草稿必須明確標示「來自歷史複製」，避免誤以為是原始新單。
- 歷史再用功能要保留原紀錄不被覆蓋，新的內容應建立為獨立草稿。

#### W. 資料匯入 / 匯出相容性規則
- 匯入不是單純讀檔，而是資料結構驗證、版本檢查、欄位對應、衝突處理四步驟。
- 每個匯入檔都要先檢查版本號、資料類型、必填欄位、是否缺少核心 ID。
- 若版本較舊，系統應自動嘗試遷移；若版本過舊或結構缺失，必須明確阻止並說明原因。
- 匯入時要區分「覆蓋」與「合併」兩種模式，並清楚告訴使用者會影響哪些資料。
- 若發現重複帳本、重複訂單、重複歷史紀錄，匯入流程應提供合併、略過、建立副本三種處理方式。
- 匯出檔應包含版本資訊、匯出時間、來源帳本 / 來源流程、資料範圍與雜湊檢查資訊。
- 備份檔與匯出檔要有不同用途標記，避免使用者把「分享用檔案」當成「可還原備份」使用。
- 匯入成功後要顯示匯入了什麼、略過了什麼、合併了什麼，不能只顯示成功兩個字。
- 匯入失敗時要顯示可修復建議，例如更新版本、改用備份檔、重新匯出。
- 所有資料交換格式都要保留向後相容空間，未來新增欄位時不能讓舊檔直接失效。

#### X. 分帳結果分享格式
- 分帳結果分享要有清楚、可讀、適合手機傳送的版型，不是原始 JSON 或超長列表。
- 分享內容至少要支援三種模板：簡短版、明細版、對帳版。
- 簡短版只保留總金額、參與人、每人應付；明細版再加上品項與分配；對帳版再加上差額與付款提醒。
- 分享內容可切換是否顯示金額、是否顯示姓名、是否顯示備註，避免不必要的公開。
- 分享結果要能複製為純文字，也要能直接丟到系統分享介面。
- 分帳分享的文案要能看出「誰欠誰多少」或「每人應付多少」，避免只是一串數字。
- 若是多人聚餐，分享格式要能快速確認是否有人尚未加入或尚未分配。
- 分享頁面應提供預覽，讓使用者先看過再送出，避免傳錯內容。
- 分享後要保留返回分帳頁或訂單頁的入口，不讓使用者失去上下文。

#### Y. 首頁「最近使用」區塊排序邏輯
- 首頁的最近使用區塊要優先顯示「上次未完成的流程」，其次顯示「最近一次完成的訂單」，再來才是一般歷史項目。
- 如果有未完成草稿，首頁應顯示明確標籤，例如「未完成的訂單」、「未完成的分帳」、「待備份資料」。
- 最近使用卡片要顯示最重要的識別資訊：店家 / 帳本名稱 / 日期 / 總額 / 狀態。
- 排序規則要可預期，不可因為某次操作就讓首頁順序忽然大亂。
- 最近使用區塊應支援快速續作，讓使用者一鍵回到上次未完成的流程。
- 若使用者沒有任何近期紀錄，首頁要改成行動引導卡，例如「開始掃描你的第一張菜單」或「建立第一個帳本」。
- 最近使用區塊不能與永久收藏混在一起，避免新近流程被收藏項目淹沒。

#### Z. 分類與標籤系統
- 分類與標籤要分開設計：分類是統計與帳務的主軸，標籤是輔助搜尋與情境標記。
- 分類應維持少而穩定，避免帳本越用越亂；標籤可以較彈性，但仍需可管理。
- 每筆資料至少可指定一個主分類，並可額外加上多個標籤。
- 分類命名要清楚，避免過度細碎，必要時支援自訂分類群組。
- 標籤應支援使用情境，例如「旅行」、「聚餐」、「報帳」、「家庭」、「公司」。
- 掃描結果、訂單、分帳、歷史紀錄都應可帶有標籤，方便跨頁搜尋。
- 分類與標籤在匯出/匯入時都要完整保留，不可只存金額與名稱。
- 帳本可有專屬分類與共用分類兩種模式，但要清楚區分哪些是帳本內部使用，哪些是全域共用。
- 若分類被停用，歷史資料仍要可讀，不得因分類刪除而讓舊紀錄失真。

#### AA. 搜尋 / 篩選 / 排序規則
- 搜尋必須覆蓋訂單、分帳、歷史、帳本與分類，不可只做單一頁面內搜尋。
- 搜尋欄應支援關鍵字、店名、帳本名、日期、金額、標籤與人名。
- 篩選器至少要有：帳本、日期區間、金額區間、分類、標籤、狀態。
- 排序至少要有：最新優先、最舊優先、金額高低、店家字母/名稱、人均金額。
- 搜尋結果要高亮命中關鍵字，讓使用者看得出為什麼被搜到。
- 篩選條件要顯示目前已套用的條件，不能讓使用者忘記自己現在在哪種查詢模式。
- 搜尋與篩選條件應可一鍵清除，避免使用者卡在看不懂的結果裡。
- 空搜尋結果要提供建議，例如改日期、改帳本、改關鍵字，而不是只顯示空白。
- 近期使用與搜尋結果不要混成同一個列表，兩者意義不同。

#### AB. 首頁資訊密度與卡片布局原則
- 首頁的資訊密度要受控，不能同時把所有功能、提醒、歷史、分析、設定全塞進去。
- 首頁主視覺應維持三層：狀態卡、主要操作卡、最近使用卡。
- 每一層卡片都應明確區隔，避免使用者無法判斷哪個是主操作、哪個是輔助資訊。
- 首頁主要操作卡數量不宜過多，優先保留 3 到 4 個最常用動作。
- 首頁卡片文案要短，避免整張首頁變成說明書。
- 重要狀態優先顯示，例如目前帳本、目前掃描模式、上次未完成流程、待備份提醒。
- 首頁如果有圖表或統計，只能放摘要，不可直接佔用過多版面影響主流程。
- 在手機畫面下，首頁要保持「看得懂、按得下、回得去」三個原則。
- 若某區塊資訊太多，應拆入次頁，不可硬塞在首頁。

#### AC. 通知 / 提醒規則
- 通知不是打擾，而是幫助使用者不漏掉重要事項；每一種提醒都要可關閉、可調整頻率。
- 至少支援四類提醒：預算提醒、未完成草稿提醒、備份提醒、長期未使用提醒。
- 預算提醒應區分輕提示、警告、超支三個層級，文案與顏色都要不同。
- 未完成草稿提醒要能直接回到對應流程，不只是告知有草稿。
- 備份提醒要能設定週期，例如每週、每月，並顯示上次備份時間。
- 長期未使用提醒要能關閉，避免變成垃圾通知。
- 通知內容要清楚說明原因與下一步，例如「日本旅行帳本本月已用 80%，點此查看明細」。
- 通知設定要支援全域與帳本層級覆蓋，避免所有提醒只能同一套規則。
- 通知觸發後應保留紀錄，讓使用者知道自己收到過什麼提醒。

#### AD. 預算與目標控制
- 預算不是只有一個總額，至少要支援帳本月預算、分類預算與期間目標。
- 每個帳本可設定月預算，並可顯示已用金額、剩餘金額與使用率。
- 分類也可設定上限，例如餐飲、交通、娛樂各自有目標值。
- 預算狀態要有視覺化，至少顯示正常、接近上限、超支三種狀態。
- 目標控制要能設定「提醒門檻」，例如達到 50%、80%、100% 時提醒。
- 預算變化要以同一帳本的歷史資料即時計算，不要手動維護多份重複資料。
- 目標可設定為金額、次數或期間總和，視帳本情境而定。
- 超支後不可只顯示紅字，還要提供可執行的建議，例如檢視本月分類排行或調整預算。
- 若帳本沒有設定預算，介面要明確顯示為「未設定」，而不是假裝沒有這個功能。

#### AE. 資料刪除與回收機制
- 刪除不能一鍵永久消失，重要資料要先進回收區，再由使用者決定是否永久刪除。
- 回收機制至少支援：交易、訂單、分帳、帳本、分類、標籤。
- 回收項目應保留刪除時間、來源帳本與可還原資訊。
- 還原時要檢查依賴關係，例如刪掉的分類是否已被新資料使用。
- 永久刪除前要明確顯示影響範圍，不可只寫「刪除後無法復原」這種空泛警告。
- 帳本刪除要區分「封存」與「真正刪除」：封存保留資料可查，刪除才進回收區。
- 清空回收區時要再次提醒，且不能用原生 confirm()。
- 若資料被引用，系統應改為「停用」或「隱藏」，避免直接刪除導致歷史失真。
- 回收區要提供搜尋、篩選與批次還原，避免資料一多就無法管理。

#### AF. 檢驗清單總表
- 每次大改版都至少要檢查：入口可達性、主流程完整性、返回路徑、空狀態、錯誤回退、資料持久化、跨模組同步、半成品暴露。
- 模組化檢查：每個功能模組是否有清楚邊界、明確責任、獨立頁面或獨立區塊、清楚狀態來源。
- 流程檢查：掃描、訂單、分帳、歷史、帳本、設定是否都能從入口走到結尾，再走回原路。
- 資料檢查：localStorage 是否能正確保存、讀取、遷移、回收、匯入、匯出。
- UI 檢查：手機上是否看得懂、按得下、回得去；是否有主次按鈕；是否沒有遮擋與死角。
- 內容檢查：文案是否明確指出下一步，失敗時是否能行動，空畫面是否有導引。
- 若某功能尚未完成，必須在檢驗清單中被標示為「不可放進主導航」或「必須明示開發中」。

#### AG. 子代理協作原則
- 子代理只負責單一明確子任務，不要同時承接太多跨頁修改。
- 每個子代理都要收到完整上下文：目標、檔案範圍、限制、驗收標準、不得改動區域。
- 子代理輸出只作為建議，必須由主代理再做一次交叉驗證，不可直接視為完成。
- 若是高風險改動，子代理至少要分成「規格檢查」與「實作檢查」兩層。
- 子代理回報需包含：修改了什麼、影響哪些頁面或資料、是否有風險、是否符合規格。
- 子代理不得自行擴張需求；若發現新問題，只能回報，不可自行改大方向。
- 子代理適合用在：模組切分、功能盤點、UI 走查、資料相容性確認、驗收條件核對。

#### AH. 子代理驗收標準
- 子代理回傳的內容必須能對照 spec，不能只寫「已完成」。
- 至少要能說清楚：影響範圍、驗證結果、未解決項、是否需要主代理接手。
- 若子代理聲稱某頁面或流程可用，主代理仍需抽查主流程一次。
- 若子代理發現規格與現況衝突，必須優先回報衝突，不可自己選擇忽略。
- 子代理產出的清單要可直接轉成後續任務，不要只是敘述性心得。

- 相機或上傳圖片掃描完成後，先進入結果頁，不直接跳走。
- 結果頁保留每個品項的名稱、翻譯、價格、說明、標籤與過敏提醒。
- 使用者可以先檢查辨識內容，必要時先改數量、收藏或移除不想要的品項。
- 頁面上要有明確的「完成 / 加入購物車」主按鈕，作為下一步入口。

### Phase 2：使用者點選完成後，將選定品項加入購物車
- 使用者按下「完成 / 加入購物車」後，把目前選中的菜單項目同步到購物車。
- 若購物車已有相同品項，則累加數量，不重複建立。
- 結果頁與購物車狀態要同步，避免一邊改了另一邊沒更新。
- 加入完成後顯示清楚回饋，讓使用者知道已完成哪一步。

### Phase 3：把購物車整理成店內可用的訂單預覽
- 購物車要能轉成一張清楚的「服務人員訂單」。
- 顯示店家/時間/品項/數量/單價/小計/總額/備註。
- 提供「返回修改」、「確認送出」、「進入分帳」的明確操作。

### Phase 4：訂單轉成分帳紀錄
- 訂單確認後可直接建立分帳紀錄。
- 支援新增人員、刪除人員、平均分帳與後續自訂調整。
- 分帳結果要能保存，重新打開時仍可看到人員與金額。

### Phase 5：保存歷史記憶
- 每張完成的訂單都存入歷史紀錄。
- 歷史可重開，查看這次吃了什麼、多少錢、跟誰分帳、每人應付多少。
- 仍需維持舊 localStorage 資料相容，不破壞原本的收藏、結果與購物車資料。

## 範圍
### 會做
- 掃描結果先呈現成可檢查的菜單清單，再由使用者按完成後加入購物車
- 購物車生成「服務人員訂單」
- 訂單可轉為「分帳紀錄」
- 分帳時可新增人員
- 訂單可保存為歷史紀錄 / 記憶
- 改善掃描頁 UX，避免卡死、誤導、半成品感
- 改善結果頁與訂單頁的層級、按鈕與空白狀態

### 不做
- 不接真正金流
- 不接餐廳後台
- 不做雲端同步
- 不做會員系統
- 不做伺服器端計算

### 7. 帳本功能：多帳本、多情境管理
#### 7.1 帳本概念
- 帳本不是單一混合總帳，而是支援多帳本 / 多情境管理。
- 使用者可以自行建立帳本，並為每個帳本設定用途，例如日常生活、旅行、家庭開銷、工作、娛樂等。
- 例如可建立一個名為「2026-06 日本旅行」的帳本，專門記錄該次旅行的全部開銷。
- 每個帳本都應有獨立的收支資料、分類、標籤、統計與圖表。
- 帳本之間彼此獨立，但也能在總覽中合併檢視。

#### 7.2 帳本建立與模板
- 建立新帳本時，提供快速模板選擇，例如：日常生活、旅行、家庭開銷、工作。
- 模板只作為初始設定，不限制後續修改。
- 帳本名稱、用途、顏色或圖示等外觀資訊可由使用者自訂。

#### 7.3 帳本封面摘要
- 每個帳本的首頁應顯示摘要資訊，至少包含：
  - 總支出
  - 本月支出
  - 最大分類
  - 最近一筆
  - 未分類項目
- 讓使用者一眼看出這個帳本目前的狀態。

#### 7.4 帳本切換與歸檔
- 使用者可快速切換目前正在使用的帳本。
- 帳本可歸檔，歸檔後仍保留查閱能力，但不干擾日常使用中的帳本列表。
- 歸檔帳本可作為歷史情境保存，例如某次旅行結束後封存。

#### 7.5 帳本內獨立分類
- 不同帳本可以有不同分類，不採全域死板共用分類。
- 例如旅行帳本可用：交通、住宿、餐飲、門票。
- 日常生活帳本可用：餐飲、交通、生活用品、娛樂。
- 分類可由使用者自訂、增刪、調整。

#### 7.6 帳本與掃描資料的關聯
- 收據掃描進帳時，應可指定要寫入哪一個帳本。
- 例如：日本旅行的收據進入「2026-06 日本旅行」帳本；超市收據進入「日常生活」帳本。
- 菜單點餐與分帳流程若需要，也可關聯到特定帳本，方便後續分析與回顧。

#### 7.7 帳本總覽與比較
- 可查看所有帳本的總覽資訊，例如各帳本花費排行、合計支出、近期使用狀態。
- 支援跨帳本比較，讓使用者知道不同情境的花費差異。

#### 7.8 驗收標準
- 使用者能建立至少 2 個以上獨立帳本。
- 帳本可依用途分類，不會混在同一個總帳裡。
- 收據與菜單資料可正確進入對應帳本。
- 帳本可切換、可歸檔、可回顧。

### 8. 帳本功能：收支紀錄與分析
#### 8.1 收支紀錄
- 每筆收支紀錄可新增、編輯、刪除。
- 記錄欄位至少應包含：日期、金額、分類、帳本、標籤、備註、來源。
- 支援手動新增，也支援從掃描資料自動建立。
- 支援收入與支出兩種基本類型。

#### 8.2 搜尋與篩選
- 可依店家、分類、日期、金額、標籤搜尋與篩選紀錄。
- 可快速查看某個帳本內的全部明細或某段時間的明細。
- 篩選結果應可直接用於後續分析。

#### 8.3 統計與圖表
- 帳本分析至少包含圓餅圖，用來看分類占比。
- 也可擴充長條圖、趨勢圖、分類排行等視覺化。
- 統計內容至少要有：
  - 本月總支出
  - 分類占比
  - 趨勢變化
  - 最大支出分類
  - 最近新增紀錄

#### 8.4 預算與提醒
- 支援為帳本或分類設定預算。
- 當接近預算上限時，顯示提醒。
- 可用於旅行、家庭開銷、日常生活等場景的控管。

#### 8.5 匯出 / 匯入
- 支援單一帳本或全部資料的匯出與匯入。
- 優先支援 JSON，必要時可加 CSV。
- 用途包含備份、換機與長期保存。

#### 8.6 重複資料偵測與修正
- 掃描收據或手動新增時，系統應提供重複資料提示。
- OCR 辨識後應允許人工修正品名、金額、分類、日期、備註，避免辨識錯誤直接進帳。

#### 8.7 驗收標準
- 使用者能對每筆紀錄進行新增、編輯、刪除。
- 使用者可在不同帳本間切換並查看對應統計。
- 圓餅圖與其他分析圖表能正確反映資料分布。
- 預算與提醒功能可正常運作。
- 匯出與匯入後資料仍可正常讀取。

## 需求分解

### 1. 掃描流程 UX 改善
#### 1.1 必要行為
- 進入掃描頁後，使用者能明確知道：
  - 現在是菜單掃描還是收據掃描
  - 主按鈕是「拍照辨識」
  - 次按鈕是「上傳照片」
  - 無法啟動相機時，必須有明顯的 fallback
- 相機權限失敗時，不可卡住在空白畫面。
- 掃描頁要有清楚的返回路徑，不可讓使用者迷路。

#### 1.2 UI 原則
- 主動作只保留一個最明顯的按鈕。
- 次動作保留上傳與說明，但視覺層級要低於主按鈕。
- 錯誤狀態要顯示「怎麼辦」，不是只顯示「錯誤」。
- 不使用原生 alert / confirm / prompt 作為主要流程。

#### 1.3 驗收標準
- 掃描頁進場時，能看懂下一步。
- 相機失敗後仍可立刻改用相簿上傳。
- 使用者能清楚返回結果頁、首頁或上一個模組頁。

### 2. 掃描後自動加入購物車
#### 2.1 核心行為
- 菜單辨識成功後，解析出的每個品項要自動進入購物車。
- 若品項已存在購物車，應累加數量，而不是重複建立相同項目。
- 使用者可在結果頁直接調整數量、刪除項目、收藏品項。

#### 2.2 資料邏輯
- 掃描結果仍保留在結果清單。
- 購物車是獨立狀態，和結果清單同步但不互相覆蓋。
- 商品名稱、原文、價格、數量、標籤、過敏提醒都應保留。

#### 2.3 驗收標準
- 掃描完成後，購物車一定有對應項目。
- 使用者不用再手動逐一把菜單加入購物車。
- 結果頁與購物車頁數量狀態一致。

### 3. 生成給服務人員看的訂單
#### 3.1 訂單定位
- 購物車不是只用來自己看，還要能生成一張「店內點餐訂單」。
- 這張訂單要適合在手機上直接給服務人員看，重點資訊要大、清楚、可快速確認。

#### 3.2 訂單內容
- 店名 / 時間 / 訂單編號
- 品項名稱（中文優先，必要時保留原文）
- 數量
- 單價與小計
- 總金額
- 備註（例如過敏、不要香菜、辣度）

#### 3.3 生成流程
- 使用者在購物車按「生成訂單」或「確認點餐」。
- 進入訂單預覽頁。
- 預覽頁應可直接：
  - 回去修改
  - 送給服務人員看
  - 確認完成並保存

#### 3.4 驗收標準
- 服務人員看到的訂單格式簡潔、可讀、不卡版面。
- 訂單可從購物車一鍵生成。
- 訂單不會因為切頁而消失。

### 4. 訂單同時作為分帳紀錄
#### 4.1 核心概念
- 每張訂單不只是餐點訂單，也是一筆分帳紀錄。
- 訂單完成後，可以直接進入分帳流程。
- 分帳結果會保存成歷史資料，之後可重看、再計算、做回顧。

#### 4.2 分帳模式
至少要支援以下兩種：
1. 平均分帳
2. 自訂分配

若開發成本允許，建議進一步支援：
- 按品項分配給某個人
- 按比例分帳
- 按金額手動調整

#### 4.3 驗收標準
- 訂單完成後可直接轉為分帳紀錄。
- 分帳紀錄要保存，不能只停留在當次畫面。
- 使用者能回頭查看每個人的應付金額。

### 5. 分帳功能：可新增人員
#### 5.1 人員管理
- 分帳頁必須可以新增人員。
- 可輸入姓名。
- 可刪除人員。
- 可重置整組分帳對象。

#### 5.2 分帳規則
- 預設先用平均分帳，讓流程簡單。
- 使用者可針對特定人員調整金額。
- 最終分帳結果需顯示：
  - 每人應付
  - 總計
  - 與原始總額差異（若有自訂調整）

#### 5.3 驗收標準
- 至少能新增 2 個以上人員。
- 分帳結果會跟著人員名單一起保存。
- 使用者重新打開歷史訂單時，人員與金額仍在。

### 6. 歷史記憶功能
#### 6.1 歷史定位
- 每次完成的訂單都要進入歷史記憶。
- 歷史功能不是只有列表，還要能回顧這張單：
  - 這次吃了什麼
  - 總共多少錢
  - 跟誰分帳
  - 每個人分多少

#### 6.2 歷史資料建議結構
每筆歷史訂單至少保存：
- id
- createdAt
- storeName 或場景名稱
- items
- totalAmount
- currency
- participants
- splitResult
- notes
- sourceScanIds / sourceResultsRef
- status（草稿 / 已確認 / 已結帳）

#### 6.3 驗收標準
- 歷史記錄重新進入後內容完整。
- 不需要再重新掃描即可看見舊單。
- 歷史資料可作為後續分帳、帳務、回顧的依據。

## 建議畫面結構
### A. 掃描頁
- 主按鈕：拍照辨識
- 次按鈕：上傳照片
- 說明：如何掃、掃不到怎麼辦
- 返回：清楚可見

### B. 結果頁
- 上方：本次掃描結果摘要
- 中段：菜單項目
- 下方：購物車摘要條
- 動作：繼續掃下一頁 / 開始新掃描 / 確認點餐

### C. 訂單預覽頁
- 大標題：服務人員訂單
- 品項清單清楚、字體大
- 總額明顯
- 按鈕：返回修改 / 進入分帳 / 儲存歷史

### D. 分帳頁
- 人員列表
- 新增人員按鈕
- 平均分帳 / 自訂分配
- 每人應付總額
- 完成後保存到歷史

### E. 歷史頁
- 依時間排序
- 顯示店名、總額、參與人數
- 點進去可看完整訂單與分帳明細

## 資料持久化建議
新增或沿用 localStorage keys 時，建議保留舊資料並新增下列項目：
- `menulens_order_history`
- `menulens_split_people`
- `menulens_split_drafts`
- `menulens_current_bill`

若需要遷移，原則是：
- 舊資料不刪
- 新資料可從舊的 order / results / finance 資料推導
- 任何遷移失敗都要回退到可讀狀態

## 非目標與風險
### 非目標
- 不做真正付款
- 不做雲端同步
- 不做多人即時協作

### 風險
- 分帳規則若太自由，UI 會複雜到不好用
- 歷史紀錄若太大，localStorage 可能膨脹
- 掃描後直接加購物車若沒做好去重，會讓數量混亂

## 驗收清單
- [ ] 掃描頁有清楚主次動作與返回路徑
- [ ] 相機失敗時可切到上傳，不會卡死
- [ ] 掃描成功後，結果會自動進購物車
- [ ] 購物車可生成一張服務人員訂單
- [ ] 訂單可以保存成分帳紀錄
- [ ] 分帳頁可以新增人員
- [ ] 分帳結果可保存並在歷史中回看
- [ ] 歷史訂單可作為記憶功能再次開啟
- [ ] 舊資料不被破壞
- [ ] 全部流程在手機上可順暢操作


## 本次修正（2026-05-30 / 下一輪）
### 修改範圍
- 訂單詳情頁：補完整內容、狀態、回到來源與重新使用入口。
- 帳本頁：補多帳本清單、帳本摘要、快速新增交易與可見的返回路徑。
- 匯出 / 匯入 / 分享：建立清楚的資料入口與兼容說明，優先保留 localStorage 相容。

### 介面變動
- 歷史清單點入後必須能看到訂單詳情，而不是只回到預覽骨架。
- 帳本中心需讓使用者看見目前帳本與下一步可做的事，不能只有空白卡片。
- 匯出 / 匯入 / 分享入口需與資料管理區塊同層顯示，並提供明確返回路徑。

### 資料結構變動
- 歷史訂單需要保留更多可回看欄位：店名/場景、品項、總額、參與者、分帳結果、來源與備註。
- 帳本資料需要支援基本的帳本清單與交易明細。
- 匯入資料需維持舊 key 相容，不能破壞現有 `menulens_*` 結構。

### 驗收標準
- 使用者可從首頁一路走到歷史詳情、再返回首頁。
- 帳本頁可看見至少一個可操作的帳本狀態，不會只剩半成品提示。
- 匯出 / 匯入 / 分享有清楚入口，不會讓使用者以為功能不存在或已壞掉。
- 所有新入口都要有清楚返回路徑與空狀態說明。

### 補充修正（2026-05-30 / 帳本操作與匯率容錯）

#### 修改範圍
- 帳本清單與帳本詳情頁。
- 交易紀錄列表與單筆紀錄操作。
- 匯率取得與收據存帳流程。

#### 介面變動
- 帳本詳情需提供：重新命名、刪除帳本、查看交易紀錄。
- 交易紀錄需提供：查看明細、編輯、刪除。
- 若目標幣別沒有已儲存匯率，收據儲存帳本時應優先嘗試自動抓取 live rate，再進行換算。

#### 資料結構變動
- 帳本需支援更新名稱與基本資訊。
- 交易紀錄需保留原始金額、換算後金額、原始幣別、目標幣別與來源，以便編輯與追蹤。

#### 驗收標準
1. 帳本可以重新命名與刪除，且畫面會即時刷新。
2. 帳本詳情頁可清楚看到每筆交易，並可對單筆交易查看、編輯、刪除。
3. 當本機沒有儲存匯率時，收據仍可嘗試抓取最新匯率並正確換算，不可直接把 JPY 原值當成 TWD 顯示或存入。
